<Window x:Class="JobSter.Views.PasswordView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JobSter.Views"
        mc:Ignorable="d"
        Title="PasswordView" Height="200" Width="300" WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0"
               Content="Enter your password:"
               Margin="0,0,0,12"
               FontSize="14"/>
        <PasswordBox Grid.Row="1"
                     MaxLength="20"
                     PasswordChar="*"
                     Padding="6"
                     FontSize="16"
                     MinWidth="220"
                     BorderThickness="1"
                     />
    </Grid>
</Window>
