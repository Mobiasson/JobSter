<Window x:Class="JobSter.Views.PasswordView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:JobSter.Views" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" mc:Ignorable="d" Title="PasswordView" Height="300" Width="300" WindowStartupLocation="CenterScreen" WindowStyle="None" ShowInTaskbar="False" ResizeMode="NoResize" BorderThickness="2" BorderBrush="#673AB7">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Border BorderBrush="#673AB7" BorderThickness="0,0,0,2" Padding="0" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="4">
            <TextBlock Text="JobSter" FontSize="20" FontWeight="SemiBold" FontStyle="Italic" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="pack://application:,,,/Resources/Noto/NotoSans-Italic.ttf#Noto Sans" />
        </Border>
        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Orientation="Horizontal">
            <Button x:Name="btn_Close" Grid.Row="0" Background="Transparent" BorderThickness="0" ToolTip="Close" Width="32" Height="32" Padding="6" Click="btn_Close_Click" Focusable="False" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#FFF0F0F0" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#FFE0E0E0" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
                <materialDesign:PackIcon Kind="Close" Foreground="#FF333333" Width="16" Height="16" />
            </Button>
        </StackPanel>
        <Label Grid.Row="1" Content="Enter your password:" FontSize="14" Margin="0,0,0,8" />
        <PasswordBox x:Name="PasswordInput" Grid.Row="2" MaxLength="20" PasswordChar="*" Padding="6" FontSize="16" MinWidth="220" BorderThickness="1" Margin="0,0,0,12" PasswordChanged="PasswordBox_PasswordChanged" />
        <Label Grid.Row="3" Content="Confirm password:" FontSize="14" Margin="0,0,0,8" />
        <PasswordBox x:Name="ConfirmPassword" Grid.Row="4" MaxLength="20" PasswordChar="*" Padding="6" FontSize="16" MinWidth="220" BorderThickness="1" Margin="0,0,0,4" PasswordChanged="PasswordBox_PasswordChanged" />
        <TextBlock x:Name="PasswordMismatchMessage" Grid.Row="5" Foreground="Red" FontSize="12" Text="Passwords do not match." Visibility="Collapsed" Margin="0,0,0,8" />
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btn_Back" Content="Back" Margin="0,0,5,0" Click="btn_Back_Click" />
            <Button x:Name="btn_Confirm" Content="Confirm" Margin="5,0,0,0" Click="btn_Confirm_Click" IsEnabled="False" />
        </StackPanel>
    </Grid>
</Window>
